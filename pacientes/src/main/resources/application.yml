spring:
    profiles:
      active: local
    datasource:
      url: jdbc:postgresql://localhost:5432/postgres
      username:
      password:
    jpa:
      hibernate:
        ddl-auto: create-drop
      show-sql: true
